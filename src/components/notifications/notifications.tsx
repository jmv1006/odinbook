const Notifications = ({toggle}: any) => {

    return(
        <div>
            Notifications Here
            <button onClick={() => toggle()}>x</button>
        </div>
    )
}

export default Notifications